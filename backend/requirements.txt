# ForgeSight backend — pip parity with pyproject.toml (for the container / submission ZIP).
# Runtime (Scenario A slice):
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0
psycopg[binary,pool]>=3.2
langgraph>=0.2.50
langgraph-checkpoint-postgres>=2.0
langchain-core>=0.3
langchain-ollama>=0.2
langchain-text-splitters>=0.3
ollama>=0.4
numpy>=2.0
pandas>=2.2
scikit-learn>=1.5
lightgbm>=4.5          # unpickle the served defect pipeline (LGBMClassifier)
# Hosted-synthesis fallback (cloud deploy where Ollama isn't reachable):
openai>=1.40
reportlab>=4.2          # §5.4 maintenance PDF reports (alert report + shift summary)
# Corpus seeding (dev-time):
pymupdf>=1.24
langchain-community>=0.3
# Datasets (dev-time):
ucimlrepo>=0.0.7
requests>=2.32
# Tests:
pytest>=8.3
