# frontend/ — Next.js 15 + CopilotKit (Phase 5, deferred to Pass 4)

Per `forgesight-v3-final.md` Part 2 and `BUILD_GUIDE.md §6`: Next.js 15 · Tailwind · shadcn/ui ·
Recharts · CopilotKit CoAgents. Build tokens + the 9 atoms first, then the **Evidence Trail**
(the signature element), then screens in scenario order.

`lib/schemas.ts` is the TS mirror of `backend/schemas/cards.py` — one schema, mirrored, never two
hand-maintained copies.
